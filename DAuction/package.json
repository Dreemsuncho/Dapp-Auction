{
  "name": "dauction",
  "version": "1.0.0",
  "description": "",
  "main": "scripts/app.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "dev": "lite-server",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:dev": "watchify src/scripts/App.js -o ./src/bundle.js -v",
    "build:prod": "watchify src/scripts/App.js -o ./dist/bundle.js -v",
    "build": "npm run build:dev && npm run build:prod"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lite-server": "^2.3.0"
  },
  "dependencies": {
    "truffle-contract": "^3.0.3"
  }
}
